{
  "name": "ai-engineering-roadmap-devcontainer",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "backend",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "docker-compose build backend && docker-compose up -d"
}
