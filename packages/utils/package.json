{
  "name": "@brainwav/utils",
  "version": "2.0.0",
  "description": "Shared utilities for brAInwav platform", 
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}